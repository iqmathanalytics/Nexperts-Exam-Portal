# Netlify — TanStack Start (@netlify/vite-plugin-tanstack-start in vite.config.ts)
# Monorepo: set Base directory = proctor-ace-ui in Netlify site settings.

[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
